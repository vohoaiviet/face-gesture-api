<UserControl x:Class="ClientFaceGestures.UI.MultimediaUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="MultimediaUserControlLoaded">
        <GroupBox Header="Video stream" Name="GroupBoxVideoStream" MinWidth="380" MinHeight="320">
            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="250*" />
                <RowDefinition Height="40" />
            </Grid.RowDefinitions>
            <Image Name="CurFrame" Margin="6" Initialized="CurFrameInitialized" Stretch="Fill" AllowDrop="True" Drop="CurFrameDrop" />
            <Slider Margin="6" Name="VideoLengthSlider" TickPlacement="Both" Grid.Row="1" IsEnabled="False" />
            </Grid>
        </GroupBox>
</UserControl>
